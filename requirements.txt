# Layer 1 — Perception
faster-whisper==1.0.3
torch==2.2.2
torchaudio==2.2.2
silero-vad
deepfilternet

# Layer 2 — Decision
anthropic>=0.109.0
openai>=1.0.0
google-generativeai
python-dotenv
Levenshtein

# Layer 3 — Metadata Bridge
opentimelineio

# Layer 4 — Execution
# DaVinci Resolve ships its own Python — no pip install needed
# ffmpeg must be installed via brew/apt separately

# API Server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.0.0
python-multipart>=0.0.9

# Utilities
ffmpeg-python
rich
typer
